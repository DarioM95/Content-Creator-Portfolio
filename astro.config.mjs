// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
	
  site: 'https://DarioM95.github.io', 
  base: '/Content-Creator-Portfolio', 
  devToolbar: {
    enabled: false,
  },
});
